# Frontend Environment Variables
# Copy this file to .env and fill in the values

# Backend API URL (default: proxied through Vite dev server)
# VITE_API_URL=http://localhost:3001

# No frontend API keys needed - all social media API calls
# are proxied through the backend to keep keys secure.
# See backend/.env.example for YouTube & Instagram API setup.
